/**
 * Session 域：SessionRouter（DEV214）。创建/定位统一 Session，维护 Personal 元数据。
 */
export { SessionRouter } from "./router";
export type { NewSessionInput, SessionRouterOptions, SessionTarget } from "./router";
